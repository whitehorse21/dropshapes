# Add your existing backend .env variables here.
# Chat assistant (Claude API) – required for the home chat
ANTHROPIC_API_KEY=your-anthropic-api-key

# Speech-to-text for voice messages (faster-whisper, local). Model size: tiny, base, small, medium, large-v2, large-v3
WHISPER_MODEL_SIZE=base
